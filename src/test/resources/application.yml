spring:
  application.name: test-finances
  datasource:
    url: jdbc:tc:postgresql:///finances?TC_REUSABLE=true
    username: admin
    password: pass
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
#  flyway.enabled: false

server:
  port: 11968
